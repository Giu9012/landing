interface UserMenuItem {
  icon: string;
  label: string;
  route?: string;
}

interface SidebarItem {
  icon: string;
  label: string;
  route?: string;
}

interface SidebarUser {
  avatar: string;
  title: string;
  info: string;
}